pipeline {
    agent any

    stages {
        stage('Compile Stage') {
            steps {
                echo 'Compile...'
            }
        }

        stage('Deployment Stage') {
            steps {
                echo 'deployment done'
            }
        }
    }
}